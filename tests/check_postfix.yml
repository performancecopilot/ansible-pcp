# SPDX-License-Identifier: MIT
---
- name: Check if postfix PMDA is registered
  shell: |-
    set -euo pipefail
    pmprobe -I pmcd.agent.status | grep -w '"postfix"'
  register: result
  until: result.stdout.find("postfix") != -1
  retries: 10
  delay: 1
