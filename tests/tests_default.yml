# SPDX-License-Identifier: MIT
---

- name: Ensure that the pcp role runs with default parameters
  hosts: all

  collections:
    - community.performancecopilot

  roles:
    - community.performancecopilot.pcp
